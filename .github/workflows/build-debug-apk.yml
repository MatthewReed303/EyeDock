name: Build Debug APK (Mock Google Services)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Make gradlew executable
      run: chmod +x gradlew

    # Create a minimal mock google-services.json to satisfy the plugin
    # This disables real Google Drive but allows the build to complete
    - name: Create mock google-services.json
      run: |
        mkdir -p app/src/debug
        cat << EOF > app/src/debug/google-services.json
        {
          "project_info": {
            "project_number": "000000000000",
            "firebase_url": "https://mock-project.firebaseio.com",
            "project_id": "mock-project",
            "storage_bucket": "mock-project.appspot.com"
          },
          "client": [
            {
              "client_info": {
                "mobilesdk_app_id": "1:000000000000:android:mockdebug",
                "android_client_info": {
                  "package_name": "com.eyedock.app.debug"
                }
              }
            }
          ]
        }
        EOF

    - name: Build debug APK
      run: ./gradlew assembleDebug

    - name: Upload debug APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: eyedock-debug-apk
        path: app/build/outputs/apk/debug/app-debug.apk
        retention-days: 7
